import 'package:flutter/material.dart';
import 'package:flutter_auth_buttons/flutter_auth_buttons.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Mirinae',
      home: Grade(),

    );
  }
}

class Grade extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.teal[200],

      body: Center(

        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          mainAxisAlignment: MainAxisAlignment.center,

          children: <Widget>[

            Column(
                mainAxisAlignment: MainAxisAlignment.center, // 주 축 기준 중앙
                children: <Widget>[
                  SizedBox(
                    height: 150.0,
                  ),

                  Text('e-mail',
                    style: TextStyle(
                      color: Colors.white,
                      fontSize: 25.0,
                      letterSpacing: 1.0,
                    ),
                  ),

                  SizedBox(
                    height: 20.0,
                  ),

                  FacebookSignInButton(onPressed: () {// call authentication logic
                  }),

                  SizedBox(
                    height: 20.0,
                  ),

                  TwitterSignInButton(
                    onPressed: () {},
                    borderRadius: 10.0,
                  ),

                  SizedBox(
                    height: 20.0,
                  ),

                  GoogleSignInButton(
                    onPressed: () {/* ... */},
                    darkMode: true, // default: false
                  ),

                  SizedBox(
                    height: 35.0,
                  ),

                  Text('Guest',
                    style: TextStyle(
                      color: Colors.white,
                      fontSize: 25.0,
                      letterSpacing: 1.0,
                    ),
                  ),

                  SizedBox(
                    height: 20.0,
                  ),

                  Text('x',
                    style: TextStyle(
                      color: Colors.white,
                      fontSize: 30.0,
                      letterSpacing: 1.0,
                    ),
                  )

                ]
            ),
            
          ],
        ),
      ),
    );
  }
}
